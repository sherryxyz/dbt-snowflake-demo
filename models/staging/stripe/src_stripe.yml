version: 2

sources:
    - name: stripe
      database: raw
      schema: stripe
      tables:
        - name: payment
          description: Raw payments data.
          config:
            freshness:
              warn_after:
                count: 7
                period: day
              error_after:
                count: 14
                period: day
            loaded_at_field: _batched_at